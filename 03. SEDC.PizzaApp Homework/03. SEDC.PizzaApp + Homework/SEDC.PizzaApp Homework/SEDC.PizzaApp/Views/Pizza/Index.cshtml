@model List<SEDC.PizzaApp.Models.Domain.Pizza>

<h2>These are our pizzas:</h2>

@*@for (int i = 0; i < Model.Count(); i++) //Model == List<Pizza>
    {
        <p>@Model[i].Name - @Model[i].Price</p>

    }*@

@foreach (Pizza pizza in Model)  // If we have many ID's with this path we can acces like we wish.
{
    <p title="Click me"><a href="@Url.Action("Details", "Pizza", new {id = pizza.Id})">@pizza.Name - @pizza.Price</a></p>
}



@*<h3>The partial View: </h3>
    <partial name="~/Views/Shared/Partials/_PizzaViewPartial.cshtml" />*@
